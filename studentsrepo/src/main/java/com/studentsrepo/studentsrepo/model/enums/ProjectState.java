package com.studentsrepo.studentsrepo.model.enums;

public enum ProjectState {
    PENDIENTE,  // Recién subido, solo lo ve el admin
    APROBADO,   // Visible para todo el público
    RECHAZADO   // No se publica
}
